version: '3'
services:
  ros2_dev:
    build: .
    volumes:
      - .:/workspace
    environment:
      - ROS_DOMAIN_ID=0
    network_mode: host
    command: tail -f /dev/null  # Keep container running
